DATA_ANALYZE = """
Ты — data scientist. Ниже уже посчитаны факты Python (не выдумывай цифры).

Найденные инсайты:
{discovery_brief}

Отчёт о качестве:
{quality_report_raw}

Связи между столбцами:
{correlations_raw}

Напиши анализ на русском (12–18 предложений):
1) пригодность данных и качество;
2) основная область / ядро категорий (города, сегменты) и что в неё не входит;
3) выбросы, аномалии, подозрительные шкалы и валюты;
4) различия групп и производные метрики;
5) по важным столбцам — по одному выводу.

Для столбцов используй отдельную строку:
**ИмяСтолбца** — краткий вывод.
Без общих фраз и без фактов, которых нет во входных данных.
"""

DATA_HYPOTHESES = """
Ты — data scientist. Гипотезы уже найдены расчётами Python. Твоя задача — сделать формулировки яснее, не меняя факты и цифры.

Инсайты:
{discovery_brief}

Готовые гипотезы (JSON, их нужно сохранить):
{python_hypotheses_json}

Правила:
- верни ВСЕ исходные гипотезы с теми же id, columns, kind, priority;
- можно улучшить title, statement, verification (язык, ясность);
- rationale оставь с исходными числами или скопируй;
- можно добавить не больше 3 новых гипотез с новыми id, только если они опираются на инсайты выше;
- не выдумывай столбцы и цифры.

Верни ТОЛЬКО:

---HYPOTHESES_START---
---JSON---
[
  {{
    "id": 1,
    "title": "краткое название",
    "statement": "если X, то Y",
    "rationale": "цифры из исходной гипотезы",
    "columns": ["столбец1"],
    "verification": "как проверить",
    "priority": "high",
    "kind": "geo_outlier"
  }}
]
---HYPOTHESES_END---

priority: только high, medium или low.
kind: geo_outlier, numeric_outlier, rare_category, group_difference, quality, concentration, correlation, derived, implausible.
Не используй одинарные кавычки внутри строк JSON.
Только валидный JSON-массив.
"""
